/*
const path = require('path');

module.exports = {
    collectCoverage: true,
    coveragePathIgnorePatterns: ['src/utils/Json.js', 'src/helpers/WindowStateHelper.js', 'src/utils/log.js'],
    coverageThreshold: {
        'src': {
            branches: 50,
            functions: 65,
            lines: 65,
            statements: 65
        }
    },
    coverageReporters: ['json', 'lcov', 'text', 'html'],
    setupTestFrameworkScriptFile: path.resolve(__dirname, 'test', 'unit', '__mocks__', 'setup.js')
};

*/
