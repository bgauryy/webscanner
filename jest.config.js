/*
const path = require('path');

module.exports = {
    collectCoverage: true,
    coveragePathIgnorePatterns: ['public'],
    coverageThreshold: {
        'src': {
            branches: 50,
            functions: 50,
            lines: 50,
            statements: 50
        }
    },
    coverageReporters: ['json', 'lcov', 'text', 'html'],
    setupFilesAfterEnv: [path.resolve(__dirname, 'test', 'unit', 'setup.js')]
}

*/
